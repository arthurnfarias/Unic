<%= form_for(estoque) do |f| %>
  <% if estoque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estoque.errors.count, "error") %> prohibited this estoque from being saved:</h2>

      <ul>
      <% estoque.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-xs-12">
    <div class="form-group col-xs-4">
        <%= f.label :nome %>
        <%= f.text_field :nome, class: "form-control", placeholder: "Insira o nome do Produto *", required: true %>
    </div>

    <div class="form-group col-xs-4">
      <%= f.label :preço %>
      <%= f.text_field :preco, class: "form-control", placeholder: "Valor do Produto *", required: true %>
    </div>

    <div class="form-group col-xs-4">
      <%= f.label :quantidade %>
      <%= f.number_field :quantidade, class: "form-control", placeholder: "Quantidade do Produto *", required: true %>
    </div>
  </div>
  <div class="col-xs-12">
    <div class="actions" >
      <%= f.submit "Cadastrar", class:"btn btn-success"%>
    </div>
  </div>  

<% end %>

